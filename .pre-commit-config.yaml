repos:
  - repo: local
    hooks:
      - id: black
        name: black
        entry: poetry run black
        language: system
        types: [python]
        args: [--line-length=100]

      - id: ruff
        name: ruff
        entry: poetry run ruff check --fix
        language: system
        types_or: [python, pyi]
